FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install dependencies (clean install)
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Run Vite dev server with host flag for external access
CMD ["npm", "run", "dev", "--", "--host"]
